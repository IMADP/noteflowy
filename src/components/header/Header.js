function Header() {
 
  return (
    <header>
      <input type='text' style={{width: '100%', height: '100%'}} placeholder="Search...(doesn't work yet)" />
    </header>
  );
} 

export default Header;
